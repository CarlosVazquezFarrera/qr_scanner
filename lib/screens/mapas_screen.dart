import 'package:flutter/material.dart';

class MapasSceen extends StatelessWidget {
  const MapasSceen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Mapas page'));
  }
}
